import estabelecimento_meios_pagamento from 'App/Models/estabelecimento_meios_pagamento'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(estabelecimento_meios_pagamento, ({ faker }) => {
  return {
    //
  }
}).build()
