import cidades_estabelecimentos from 'App/Models/cidades_estabelecimentos'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(cidades_estabelecimentos, ({ faker }) => {
  return {
    //
  }
}).build()
