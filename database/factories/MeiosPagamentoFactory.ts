import meios_pagamento from 'App/Models/meios_pagamento'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(meios_pagamento, ({ faker }) => {
  return {
    //
  }
}).build()
