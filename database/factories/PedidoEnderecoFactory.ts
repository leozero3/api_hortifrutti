import pedido_endereco from 'App/Models/pedido_endereco'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(pedido_endereco, ({ faker }) => {
  return {
    //
  }
}).build()
